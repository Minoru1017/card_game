/// <summary>背包卡牌詳情「怪物牌熟練度」列排版常數（避免狀態字與 ? 說明鈕重疊）。</summary>
public static class BackpackInspectMasteryLayout
{
    public const float BarHeight = 120f;
    public const float Inset = 12f;
    public const float HeaderHeight = 42f;
    public const float HelpButtonSizePx = 44f;
    /// <summary>狀態文字右緣與 ? 按鈕左緣之間的間距（含按鈕寬度預留）。</summary>
    public const float StatusRightReservePx = HelpButtonSizePx + 22f;
    public const float TrackHeight = 22f;
}
