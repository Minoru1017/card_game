---
name: 平日／Agent：只改腳本
about: 給 Cursor Agent 或遠端協作；限定 C# 腳本，不改場景與 Prefab
title: "[agent] "
labels: []
---

## Unity 與分支

- **Unity**：2023.1.22f1（與 `ProjectSettings/ProjectVersion.txt` 一致）
- **開 PR 建議從**：`main`（或團隊約定的預設分支）

## 這個 Issue 允許改什麼？

只動下列範圍（刪掉不適用的行）：

- [ ] `Assets/Scripts/**`
- [ ] 其他（請寫路徑）：

## 明確不要動（留給在家／編輯器）

- [ ] 不改 `.unity` 場景檔內容（檔案已存在即可）
- [ ] 不改 Prefab（`.prefab`）
- [ ] 不改圖片／Animator／Timeline（另有約定除外）

---

## Issue 類型（擇一）

- [ ] **Bug／維護**
- [ ] **功能／重構**

---

### 功能／重構時請填

#### 背景／要做什麼（一句話）

> 例：場景殼已建好，請把載入與返回接到既有導航。

#### 場景與建置（若本次會 LoadScene／切場景；不涉及則整段刪或寫「無」）

| 項目 | 請填（與專案一致） |
|------|---------------------|
| **載入目標場景名**（`SceneManager` 字串，通常同檔名不含 `.unity`） | |
| **場景資產路徑** | `Assets/Scenes/....unity` |
| **已加入 Build Settings** | [ ] 是（本次 Issue 建立前已由你勾選） |

> **命名**：場景名請與 `Assets/Scenes` 下檔名一致（大小寫一併對齊），避免與程式字串不符。

#### 完成條件（驗收，可勾選）

- [ ] 
- [ ] 
- [ ] Console **無 Error**（允許的 Warning 請寫在「補充」）

#### 操作流程

**正向（怎麼進目標場景）**

1. 
2. 

**返回／離開（若需要驗收「回上一場景／大廳」；不需要則刪本段）**

1. 
2. 

**預期行為（規格，一段話）**

> 例：僅改集中設定處即可換「下一個」場景名；或某操作後作用中場景為 `Gameprogress`。

---

### Bug／維護時請填

#### 環境

- **發生場景**（檔名或 `Assets/Scenes/...`）：
- **Build／Editor**：Editor 播放 □　Standalone □（可複選）

#### 重現步驟（愈短愈好，務必可重複）

1. 
2. 

#### 預期行為

#### 實際行為

> 請貼 Console **第一則** Error 全文，或註明「無紅字但畫面／流程錯」。

---

## 補充（選填）

- **相關腳本／類別**：
- **參考 commit／PR／Issue**：
- **已知限制**（例如：某 UI 文案尚未定稿）：
